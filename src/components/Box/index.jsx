import React from 'react'

const Box = ({ children }) => {
    return (
        <div style={{ marginRight: "30rem", marginLeft: "30rem" }}>
            {children}
        </div>
    )
}

export default Box